from services.camera_registry.models import (
    User, Role, Department, Camera, CameraSource, CameraHealth,
    CameraAdaptiveProfile, CameraCapability, DetectionEvent,
    VehicleRead, WatchlistEntity, WatchlistAlias, Alert, AuditLog
)
